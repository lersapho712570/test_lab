- name: TEMP2 
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Print
      debug:
         msg: "LER-SAK PHONANG"

    - name: Collection instance info
      os_server_info:
        #cloud: "5f64-project"
        devstack: "5f64-project"
      register: instances
